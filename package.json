{
  "name": "vruttaant-app",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "mobile:size-check": "bash scripts/check-mobile-bundle-size.sh",
    "mobile:size-trend-check": "bash scripts/check-mobile-size-trend.sh",
    "mobile:publish-apk": "bash scripts/publish-mobile-apk.sh",
    "mobile:apk-sync-check": "bash scripts/check-mobile-apk-sync.sh"
  },
  "lint-staged": {
    "backend/src/**/*.js": "npx --prefix backend eslint --no-warn-ignored --config backend/eslint.config.js"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1"
  }
}
