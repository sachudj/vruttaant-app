{
  "name": "vruttaant-app",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/src/**/*.js": "npx --prefix backend eslint --no-warn-ignored --config backend/eslint.config.js",
    "mobile_app/**/*.dart": "dart format --output=write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.4"
  }
}
